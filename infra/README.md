# Infrastructure

Local infrastructure is defined in the root `docker-compose.yml`.

Services:

- PostgreSQL with PostGIS on port `5432`
- Redis on port `6379`

Future infrastructure files should live here:

- staging and production environment notes
- CI/CD pipeline definitions
- object storage configuration
- queue worker deployment notes
- observability dashboards and alert rules
